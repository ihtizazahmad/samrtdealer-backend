// if(data){
//     const customerE=await customer.findById(customerId)
//     const mail=await sendMail(customerE.Email, "Thanks Message", `<h2>Thanks for Visiting Out Store</h2>`);
    
//  return  res.status(200).json({message:"orderdata saved to database and thanks Emailsend to customer"},data,mail)
// }